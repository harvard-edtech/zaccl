import assert from 'assert';

// import class to test
const RuleMap = require('../../helpers/ruleMap');

describe('helpers > ruleMap', function () {
	it('formats a rule correctly', function () {

	});

	it('finds a matching rule', function () {

	});
});